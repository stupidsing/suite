using external STANDARD >>
define nothing := atom:Nothing >>
define liftm :=
	fun => m0 => (do >> fun {call {m0}})
>>
define liftm2 :=
	fun => m0 => m1 => (do >> fun {call {m0}} {call {m1}})
>>
define appendm :=
	liftm2 {append}
>>
define concatm :=
	fold-right {appendm} {do >> ()}
>>
define seqm :=
	liftm2 {(:t => any -> :t -> :t) of
		atom:INTRN!MonadIntrinsics.seq | getintrn | callintrn2
	}
>>
define seqsm :=
	fold-right {seqm} {do >> nothing}
>>
define whilem :=
	cond => loop => (do >>
		if (call {cond})
		then (call {loop # whilem {cond} {loop}})
		else nothing
	)
>>
